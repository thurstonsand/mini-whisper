# Xcode
.build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/

# SwiftPM
.swiftpm/
# Research harnesses under docs/ are throwaway; the app's own lockfiles are tracked
# so releases resolve to the exact dependency versions that were tested.
docs/**/Package.resolved

# macOS
.DS_Store

# IDEs
*.swp
*~

# LSP build server config
buildServer.json
.bundle/

# Archives & build products
dist/
*.ipa
*.dSYM.zip
*.dSYM
*.profraw

# AI
/docs/scratch

# Secrets
.env
